# glowing-shell-script
This repo will have bash shell script.

## Integration Script

 * create-tar.sh was used to integrate base rootfs of an embedded linux and the application built for running on top of the base rootfs.
